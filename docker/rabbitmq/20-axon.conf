# rabbitmq:4.0-management enables the management plugin by default.
# Keep these settings explicit to avoid upgrade surprises in RabbitMQ 4.x.
deprecated_features.permit.management_metrics_collection = true

# Stop using deprecated management metrics collection mode.
# The management UI remains available with reduced stats detail.
management.disable_stats = true
