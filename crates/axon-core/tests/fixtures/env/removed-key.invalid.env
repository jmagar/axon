# Illustrates the "removed key must fail with actionable replacement
# guidance" contract rule (docs/pipeline-unification/schemas/
# config-schema.md's "Removed Key Contract" — its own worked example uses
# this exact key/replacement pair).
#
# NOT WIRED to a test (2026-07-12 alignment audit, bead axon_rust-ldozg): no
# removed-env-key registry or runtime rejection for stale `.env` keys exists
# in axon-core yet (the removed-surface drift check in
# xtask/src/schemas/registry.rs::REMOVED_SURFACE_RULES only gates *generated
# docs*, e.g. rejecting AXON_MCP_HTTP_TOKEN from reappearing in
# docs/reference/config/, not a live `.env` loaded by the running binary).
# Content-only fixture for now.
QDRANT_URL=http://127.0.0.1:53333
TEI_URL=http://127.0.0.1:52000
AXON_MCP_HTTP_TOKEN=replaced-by-AXON_HTTP_TOKEN
