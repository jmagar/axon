# Minimal valid .env fixture — see docs/pipeline-unification/schemas/
# config-schema.md's "Validation Fixtures" and
# docs/pipeline-unification/configuration/env-contract.md's "Target Minimal
# Example". Boots a minimal local Axon (Qdrant + TEI only).
#
# NOT WIRED to a test (2026-07-12 alignment audit, bead axon_rust-ldozg): no
# env-schema validator exists in axon-core yet — env vars are read ad hoc via
# `std::env::var` scattered across config building, with no single
# `.env`-parsing entry point to assert against. Content-only fixture for now.
QDRANT_URL=http://127.0.0.1:53333
TEI_URL=http://127.0.0.1:52000
