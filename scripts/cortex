#!/usr/bin/env bash
set -euo pipefail

REPO="/home/jmagar/workspace/axon_rust"
CLI_ENV="$REPO/.env"

if [[ -f "$CLI_ENV" ]]; then
  set -a
  # shellcheck disable=SC1090
  source "$CLI_ENV"
  set +a
fi

exec cargo run -q --manifest-path "$REPO/Cargo.toml" --bin cortex -- "$@"
