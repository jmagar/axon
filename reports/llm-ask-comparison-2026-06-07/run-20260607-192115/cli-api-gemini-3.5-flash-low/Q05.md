## Q05

**Question:** OpenZFS special allocation classes can store metadata and optionally small blocks. What has to exist in a pool before special-class devices can be added, how is small-block placement enabled, and why should the special device's redundancy match the pool?

**Provider:** `https://cli-api.tootie.tv/v1`  
**Model:** `gemini-3.5-flash-low`  
**Elapsed:** `5.989s`  
**Exit code:** `0`

---

Conversation
  You: OpenZFS special allocation classes can store metadata and optionally small blocks. What has to exist in a pool before special-class devices can be added, how is small-block placement enabled, and why should the special device's redundancy match the pool?
  Assistant:
Before special-class devices can be added, a pool must always have at least one normal (non-dedup/non-special) virtual device (vdev) [S1]. 

Small-block placement in the special allocation class is an opt-in feature [S1]. It is enabled by setting the `special_small_blocks` property to a nonzero value for a dataset, which allows blocks smaller than or equal to the specified threshold size to be allocated in the special class [S1][S8]. 

The redundancy of the special device should match the redundancy of the other normal devices in the pool because the special device is dedicated to allocating internal metadata [S1]. Since ZFS pools require redundant configurations (like mirrors or raidz groups) to survive device failures and automatically repair silent data corruption, a failure or corruption on a special device without matching redundancy can result in corrupted metadata and a faulted pool [S1].

Gaps: The sources do not explicitly state a single direct rule explaining why the redundancy must match, other than explaining that the special vdev contains metadata and that metadata corruption or device failures without sufficient replicas will fault the pool.

## Sources
[S1] https://openzfs.github.io/openzfs-docs/man/v2.3/7/zpool

---

Normalized answer (stored for JSON and follow-up sessions):

Before special-class devices can be added, a pool must always have at least one normal (non-dedup/non-special) virtual device (vdev) [S1]. 

Small-block placement in the special allocation class is an opt-in feature [S1]. It is enabled by setting the `special_small_blocks` property to a nonzero value for a dataset, which allows blocks smaller than or equal to the specified threshold size to be allocated in the special class [S1][S2]. 

The redundancy of the special device should match the redundancy of the other normal devices in the pool because the special device is dedicated to allocating internal metadata [S1]. Since ZFS pools require redundant configurations (like mirrors or raidz groups) to survive device failures and automatically repair silent data corruption, a failure or corruption on a special device without matching redundancy can result in corrupted metadata and a faulted pool [S1].

Gaps: The sources do not explicitly state a single direct rule explaining why the redundancy must match, other than explaining that the special vdev contains metadata and that metadata corruption or device failures without sufficient replicas will fault the pool.

## Sources
- [S1] https://openzfs.github.io/openzfs-docs/man/v2.3/7/zpoolconcepts.7.html
- [S2] https://openzfs.github.io/openzfs-docs/man/master/7/zfsprops.7.html

  Timing: retrieval=999ms | context=3ms | llm=4919ms | total=5928ms | ttft=5584ms | streamed=yes
  Session: codex-latest-live-final-20260515181733
  Warnings: Collapsed 33 near-duplicate source chunk(s) before context selection (e.g. https://openzfs.github.io/openzfs-docs/man/v2.2/7/zpoolconcepts.7.html ≈ https://openzfs.github.io/openzfs-docs/man/v2.3/7/zpoolconcepts.7.html)
