{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "axon",
  "version": "4.7.0",
  "description": "Spider-powered self-hosted RAG engine \u2014 scrape, map, extract, crawl, embed, and query via MCP or CLI. 16 skills covering web crawling, GitHub/Reddit/YouTube ingest, semantic vector search, and grounded LLM answers over indexed content.",
  "author": {
    "name": "Jacob Magar",
    "email": "jmagar@gmail.com",
    "url": "https://github.com/jmagar"
  },
  "homepage": "https://github.com/jmagar",
  "repository": "https://github.com/jmagar/axon",
  "keywords": [
    "rag",
    "crawl",
    "scrape",
    "embed",
    "qdrant",
    "semantic-search",
    "mcp",
    "spider",
    "ingest",
    "hybrid-search"
  ],
  "skills": "./plugins/skills",
  "agents": "./plugins/agents/researcher.md",
  "hooks": "./plugins/hooks/hooks.json",
  "mcpServers": "./plugins/.mcp.json",
  "userConfig": {
    "api_token": {
      "type": "string",
      "title": "API token (optional)",
      "description": "Static bearer token for programmatic access. Leave blank when using OAuth (the server handles auth via Google OAuth — set AXON_MCP_AUTH_MODE=oauth on the server). Only needed for non-browser clients that cannot do OAuth.",
      "sensitive": true,
      "required": false
    },
    "server_url": {
      "type": "string",
      "title": "Axon server URL",
      "description": "Base URL for the shared Axon Docker server. Local Docker setup uses the default.",
      "default": "http://127.0.0.1:8001"
    },
    "tavily_api_key": {
      "type": "string",
      "title": "Tavily API key",
      "description": "Optional key for search and research commands.",
      "sensitive": true
    },
    "github_token": {
      "type": "string",
      "title": "GitHub token",
      "description": "Optional token for higher GitHub ingest rate limits.",
      "sensitive": true
    },
    "reddit_client_id": {
      "type": "string",
      "title": "Reddit client ID",
      "description": "Optional Reddit ingest credential."
    },
    "reddit_client_secret": {
      "type": "string",
      "title": "Reddit client secret",
      "description": "Optional Reddit ingest credential.",
      "sensitive": true
    },
    "auth_mode": {
      "type": "string",
      "title": "MCP auth mode",
      "description": "Optional: set to oauth to enable lab-auth Google OAuth/JWT mode. Static bearer token mode is the default.",
      "default": "bearer"
    },
    "public_url": {
      "type": "string",
      "title": "OAuth public URL",
      "description": "Required only for OAuth mode; public URL used in protected-resource metadata."
    },
    "google_client_id": {
      "type": "string",
      "title": "Google OAuth client ID",
      "description": "Required only for OAuth mode."
    },
    "google_client_secret": {
      "type": "string",
      "title": "Google OAuth client secret",
      "description": "Required only for OAuth mode.",
      "sensitive": true
    },
    "auth_admin_email": {
      "type": "string",
      "title": "OAuth admin email",
      "description": "Required only for OAuth mode."
    }
  }
}
