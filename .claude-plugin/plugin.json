{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "axon",
  "description": "Spider-powered self-hosted RAG engine — scrape, map, extract, crawl, embed, and query via MCP or CLI. 16 skills covering web crawling, GitHub/Reddit/YouTube ingest, semantic vector search, and grounded LLM answers over indexed content.",
  "author": {
    "name": "Jacob Magar",
    "email": "jmagar@gmail.com",
    "url": "https://github.com/jmagar"
  },
  "homepage": "https://github.com/jmagar",
  "repository": "https://github.com/jmagar/axon",
  "keywords": [
    "rag",
    "crawl",
    "scrape",
    "embed",
    "qdrant",
    "semantic-search",
    "mcp",
    "spider",
    "ingest",
    "hybrid-search"
  ],
  "skills": "./plugins/skills",
  "agents": "./plugins/agents/researcher.md",
  "hooks": "./plugins/hooks/hooks.json",
  "mcpServers": "./plugins/.mcp.json",
  "userConfig": {
    "api_token": {
      "type": "string",
      "title": "API token",
      "description": "Bearer token for MCP authentication (AXON_MCP_HTTP_TOKEN).",
      "sensitive": true,
      "required": true
    },
    "server_url": {
      "type": "string",
      "title": "MCP server URL",
      "description": "axon MCP HTTP server base URL. Leave as default unless running on a remote host.",
      "default": "http://localhost:8001"
    },
    "mcp_host": {
      "type": "string",
      "title": "MCP bind host",
      "description": "Network interface to bind the MCP HTTP server to. Keep 127.0.0.1 unless you set AXON_MCP_HTTP_TOKEN (non-loopback requires auth).",
      "default": "127.0.0.1"
    },
    "mcp_port": {
      "type": "number",
      "title": "MCP HTTP port",
      "description": "Port for the axon MCP HTTP server.",
      "default": 8001
    },
    "qdrant_url": {
      "type": "string",
      "title": "Qdrant URL",
      "description": "URL of your Qdrant vector store (e.g. http://localhost:53333)",
      "required": true,
      "default": "http://localhost:53333"
    },
    "tei_url": {
      "type": "string",
      "title": "TEI embedding server URL",
      "description": "URL of your Text Embeddings Inference server (e.g. http://localhost:52000)",
      "required": true,
      "default": "http://localhost:52000"
    },
    "collection": {
      "type": "string",
      "title": "Qdrant collection name",
      "description": "Collection to read and write vectors (default: cortex)",
      "default": "cortex"
    },
    "openai_base_url": {
      "type": "string",
      "title": "LLM base URL",
      "description": "OpenAI-compatible LLM endpoint URL (used for ask, research, extract)"
    },
    "openai_api_key": {
      "type": "string",
      "title": "LLM API key",
      "description": "API key for the LLM endpoint",
      "sensitive": true
    },
    "openai_model": {
      "type": "string",
      "title": "LLM model name",
      "description": "Model identifier passed to the LLM endpoint"
    },
    "tavily_api_key": {
      "type": "string",
      "title": "Tavily API key",
      "description": "API key for Tavily web search (required for search/research commands)",
      "sensitive": true
    },
    "chrome_remote_url": {
      "type": "string",
      "title": "Chrome remote URL",
      "description": "Headless Chrome management API URL (e.g. http://localhost:6000)",
      "default": "http://localhost:6000"
    }
  }
}
