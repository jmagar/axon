{
  "mcpServers": {
    "axon": {
      "type": "http",
      "url": "${user_config.server_url:-http://127.0.0.1:8001}/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.api_token}"
      }
    }
  }
}
