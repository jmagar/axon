# Temporary file-level exceptions for monolith policy.
# One repo-relative path per line, no globs.
# Add a ticket/date in comments when possible, then remove quickly.
# Temporary file-level exceptions for monolith policy.
# One repo-relative path per line, no globs.
# Every entry MUST have an expiry date -- CI fails without one.
# Max 38 days out (extended for extraction sprint). Expired entries also fail CI.
#
# Format:
#   crates/core/massive_module.rs  # expires: 2026-03-09

# Pulse shell redesign -- split pending, needs extraction sprint (extended 2026-03-23)
apps/web/app/jobs/[id]/job-detail-ui.tsx  # expires: 2026-04-30
apps/web/components/shell/axon-shell-state.ts  # expires: 2026-04-30
crates/cli/commands/common.rs  # expires: 2026-04-30
crates/jobs/refresh/url_processor.rs  # expires: 2026-04-30
apps/web/hooks/ws-messages/provider.ts  # expires: 2026-04-30

# Oversized Rust files pending extraction sprint -- added 2026-03-23
crates/crawl/scrape.rs  # expires: 2026-04-30  -- 879L; needs split into scrape/{fetch,transform,pipeline}.rs
crates/core/config/parse.rs  # expires: 2026-04-30  -- 796L; needs split into parse/{args,env,merge}.rs
crates/core/config/parse/build_config.rs  # expires: 2026-04-30  -- 737L; needs split into build_config/{crawl,embed,jobs}.rs
crates/services/system.rs  # expires: 2026-04-30  -- 658L; needs split into system/{health,metrics,diagnostics}.rs
crates/crawl/engine/sitemap.rs  # expires: 2026-04-30  -- 647L; needs split into sitemap/{discover,backfill,filter}.rs
crates/core/logging.rs  # expires: 2026-04-30  -- 612L; needs split into logging/{format,output,init}.rs
crates/jobs/refresh.rs  # expires: 2026-04-30  -- 591L; needs split into refresh/{schedule,processor,state}.rs
crates/core/config/types/config.rs  # expires: 2026-04-30  -- 527L; needs split into config/types/{crawl,embed,job}.rs

crates/services/types/acp.rs  # expires: 2026-04-30  -- 547L+; Sse variant + headers pending split
crates/services/acp/session.rs  # expires: 2026-04-30  -- 530L; mcp_servers fallback fix + tests
