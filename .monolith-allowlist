# Temporary file-level exceptions for monolith policy.
# One repo-relative path per line, no globs.
# Every entry MUST have an expiry date -- CI fails without one.
# Max 38 days out. Expired entries also fail CI.
#
# Format:
#   path/to/file.rs  # expires: YYYY-MM-DD | owner: <name> | split: <plan>

# Rust files still over 500 lines -- needs splitting
src/services/query.rs  # expires: 2026-07-11 | owner: jmagar | split: query/{ask,retrieve,suggest}.rs
src/jobs/workers/runners/ingest.rs  # expires: 2026-07-17 | owner: jmagar | split: ingest/{merge,progress}.rs (payload-merge helpers + progress persister)

# Vendored lab-auth source (upstream pinned SHA 87cec324).
# Cannot be split — upstream code vendored to resolve libsqlite3-sys link conflict.
# Remove when lab upgrades to rusqlite 0.32+.
vendor/lab-auth/src/authorize.rs  # expires: 2026-07-20 | owner: jmagar | split: N/A (upstream vendored)
vendor/lab-auth/src/sqlite.rs     # expires: 2026-07-20 | owner: jmagar | split: N/A (upstream vendored)
vendor/lab-auth/src/google.rs     # expires: 2026-07-20 | owner: jmagar | split: N/A (upstream vendored)
vendor/lab-auth/src/middleware.rs  # expires: 2026-07-20 | owner: jmagar | split: N/A (upstream vendored)
