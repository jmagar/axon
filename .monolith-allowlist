# Temporary file-level exceptions for monolith policy.
# One repo-relative path per line, no globs.
# Add a ticket/date in comments when possible, then remove quickly.
# Temporary file-level exceptions for monolith policy.
# One repo-relative path per line, no globs.
# Every entry MUST have an expiry date — CI fails without one.
# Max 7 days out. Expired entries also fail CI.
#
# Format:
#   crates/core/massive_module.rs  # expires: 2026-03-09

# Pulse shell redesign — split pending, needs extraction sprint
apps/web/app/jobs/[id]/job-detail-ui.tsx  # expires: 2026-03-22
apps/web/components/shell/axon-shell-state.ts  # expires: 2026-03-22
crates/cli/commands/common.rs  # expires: 2026-03-22
crates/jobs/refresh/url_processor.rs  # expires: 2026-03-22
apps/web/hooks/ws-messages/provider.ts  # expires: 2026-03-22
