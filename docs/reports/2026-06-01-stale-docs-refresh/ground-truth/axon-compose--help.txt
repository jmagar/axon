Manage the local Docker Compose service stack

Usage
  axon compose [OPTIONS]
  axon compose <COMMAND>

Commands
  up                   Pull and start the Docker service stack
  down                 Stop the Docker service stack
  restart              Restart running services
  rebuild              Rebuild the Axon image and start the stack

Options
  -h, --help                       Print help
