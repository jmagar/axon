# Retrieve Implementation Notes

`axon retrieve` resolves URL variants, fetches matching Qdrant chunks, orders them by chunk index, and reconstructs stored text.

See [`docs/commands/retrieve.md`](../commands/retrieve.md) for CLI usage.
