# Embed Implementation Notes

`axon embed` chunks local or URL content, embeds it through TEI, and writes vectors plus payload metadata to Qdrant.

See [`docs/commands/embed.md`](../commands/embed.md) for CLI usage.
