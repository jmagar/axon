# Evaluate Implementation Notes

`axon evaluate` runs RAG and baseline answer generation, then asks Gemini to judge the results against retrieved reference material.

See [`docs/commands/evaluate.md`](../commands/evaluate.md) for CLI usage.
