{
  "description": "Axon infrastructure dependency checks",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "command -v docker >/dev/null 2>&1 && docker compose ps --status running 2>/dev/null | grep -Eq '(axon-|firecrawl)' || echo '⚠️  Axon services not running. Run: docker compose up -d'"
          }
        ]
      }
    ]
  }
}
