# =============================================================================
# .cargo/config.toml — Cargo workspace configuration for axon_rust
#
# Most build settings (linker, profile tuning, jobs) live in the developer's
# ~/.cargo/config.toml. This file only adds workspace-local settings that the
# global config cannot provide.
#
# See docs/contributing/rust.md for expected global config and system tool requirements.
# See https://github.com/jmagar/rmcp-template/blob/main/docs/contributing/rust.md for the
# canonical family reference.
# =============================================================================

# sccache: axon uses the global wrapper (~/.cargo/config.toml) = LOCAL sccache.
# No repo bypass is needed: distributed compilation (sccache-dist) is DISABLED
# fleet-wide (2026-06-02) because its build-server sandbox can't load sqlx_macros'
# libssl.so.3 (native-linking-proc-macro limitation; same class as lab's boa_engine).
# Local sccache compiles + caches axon fine (dookie has libssl). The old cross-
# worktree cache-collision rationale is also obsolete (path-sensitive keys).
# See ~/.config/sccache/config and ~/docs/sccache-setup.md. If dist is ever
# re-enabled, restore `[build]` + `rustc-wrapper = ""` here.

[alias]
# The `cargo xtask` convention: `cargo xtask <cmd>` delegates to the xtask crate.
# See: https://github.com/matklad/cargo-xtask
xtask = "run --package xtask --"

[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
