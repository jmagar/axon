<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!--
        AGP 8.7.0 + Kotlin 2.1.0 crashes inside
        androidx.lifecycle.lint.NonNullableMutableLiveDataDetector before a
        baseline can be applied:
        "Found class KaCallableMemberCall, but interface was expected".
        Keep this as a single-issue app-local suppression until the Android
        and Aurora composite builds can move to an AGP/Kotlin pair where the
        detector runs normally.
    -->
    <issue id="NullSafeMutableLiveData" severity="ignore" />
</lint>
